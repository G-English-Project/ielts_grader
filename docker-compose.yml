version: '3.8'

services:
  app:
    image: huyhoang204205/ielts-grader:latest
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./service-account.json:/app/service-account.json:ro
    restart: unless-stopped